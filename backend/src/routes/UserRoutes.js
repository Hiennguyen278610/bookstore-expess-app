// UserRoutes - TODO: Define API endpoints for users
// GET /users, POST /users, PUT /users/:id, DELETE /users/:id
import express from 'express';
const router = express.Router();
export default router;
