"use client";
export default function HomeLayout({
                                       login,
                                       children,
                                   }: Readonly<{
    login: React.ReactNode;
    children: React.ReactNode;
}>) {
    return (
        login
    )
}